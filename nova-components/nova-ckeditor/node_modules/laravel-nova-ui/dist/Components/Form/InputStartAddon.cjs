'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const vue = require('vue');
const utils = require('../../utils.cjs');
const useInputAddonStyles = require('./useInputAddonStyles.cjs');

const {
  start
} = useInputAddonStyles.useInputAddonStyles();
const InputStartAddon = /* @__PURE__ */ vue.defineComponent({
  setup(_, {
    slots
  }) {
    return () => vue.createVNode("span", {
      "class": start
    }, {
      default: () => utils.getValidChildren(slots)
    });
  }
});

exports.default = InputStartAddon;
