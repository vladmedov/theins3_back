'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const vue = require('vue');

const _sfc_main = /* @__PURE__ */ vue.defineComponent({
  __name: "Td",
  props: {
    first: { type: Boolean },
    last: { type: Boolean }
  },
  setup(__props) {
    const props = __props;
    const classes = vue.computed(() => {
      const base = "text-sm px-2 py-2 whitespace-nowrap bg-white dark:bg-gray-800 group-hover:bg-gray-50 dark:group-hover:bg-gray-900";
      const first = "w-[1%] pl-5 pr-5";
      const last = "w-[1%] text-right";
      return props.first ? `${base} ${first}` : props.last ? `${base} ${last}` : base;
    });
    return (_ctx, _cache) => {
      return vue.openBlock(), vue.createElementBlock("td", {
        class: vue.normalizeClass(classes.value)
      }, [
        vue.renderSlot(_ctx.$slots, "default")
      ], 2);
    };
  }
});

exports.default = _sfc_main;
