'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const _commonjsHelpers = require('../../_virtual/_commonjsHelpers.cjs');
const capitalize$1 = require('./capitalize.cjs');
const _createCompounder = require('./_createCompounder.cjs');

var capitalize = capitalize$1.capitalize_1,
    createCompounder = _createCompounder._createCompounder;

/**
 * Converts `string` to [camel case](https://en.wikipedia.org/wiki/CamelCase).
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category String
 * @param {string} [string=''] The string to convert.
 * @returns {string} Returns the camel cased string.
 * @example
 *
 * _.camelCase('Foo Bar');
 * // => 'fooBar'
 *
 * _.camelCase('--foo-bar--');
 * // => 'fooBar'
 *
 * _.camelCase('__FOO_BAR__');
 * // => 'fooBar'
 */
var camelCase = createCompounder(function(result, word, index) {
  word = word.toLowerCase();
  return result + (index ? capitalize(word) : word);
});

var camelCase_1 = camelCase;

const camelCase$1 = /*@__PURE__*/_commonjsHelpers.getDefaultExportFromCjs(camelCase_1);

exports.default = camelCase$1;
