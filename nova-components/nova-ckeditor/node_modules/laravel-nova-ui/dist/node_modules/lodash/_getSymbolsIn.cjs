'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _arrayPush = require('./_arrayPush.cjs');
const _getPrototype = require('./_getPrototype.cjs');
const _getSymbols = require('./_getSymbols.cjs');
const stubArray$1 = require('./stubArray.cjs');

var arrayPush = _arrayPush._arrayPush,
    getPrototype = _getPrototype._getPrototype,
    getSymbols = _getSymbols._getSymbols,
    stubArray = stubArray$1.stubArray_1;

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeGetSymbols = Object.getOwnPropertySymbols;

/**
 * Creates an array of the own and inherited enumerable symbols of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of symbols.
 */
var getSymbolsIn = !nativeGetSymbols ? stubArray : function(object) {
  var result = [];
  while (object) {
    arrayPush(result, getSymbols(object));
    object = getPrototype(object);
  }
  return result;
};

var _getSymbolsIn = getSymbolsIn;

exports._getSymbolsIn = _getSymbolsIn;
