'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _baseIsSet = require('./_baseIsSet.cjs');
const _baseUnary = require('./_baseUnary.cjs');
const _nodeUtil = require('./_nodeUtil.cjs');

var baseIsSet = _baseIsSet._baseIsSet,
    baseUnary = _baseUnary._baseUnary,
    nodeUtil = _nodeUtil._nodeUtilExports;

/* Node.js helper references. */
var nodeIsSet = nodeUtil && nodeUtil.isSet;

/**
 * Checks if `value` is classified as a `Set` object.
 *
 * @static
 * @memberOf _
 * @since 4.3.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a set, else `false`.
 * @example
 *
 * _.isSet(new Set);
 * // => true
 *
 * _.isSet(new WeakSet);
 * // => false
 */
var isSet = nodeIsSet ? baseUnary(nodeIsSet) : baseIsSet;

var isSet_1 = isSet;

exports.isSet_1 = isSet_1;
