'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _baseIsMap = require('./_baseIsMap.cjs');
const _baseUnary = require('./_baseUnary.cjs');
const _nodeUtil = require('./_nodeUtil.cjs');

var baseIsMap = _baseIsMap._baseIsMap,
    baseUnary = _baseUnary._baseUnary,
    nodeUtil = _nodeUtil._nodeUtilExports;

/* Node.js helper references. */
var nodeIsMap = nodeUtil && nodeUtil.isMap;

/**
 * Checks if `value` is classified as a `Map` object.
 *
 * @static
 * @memberOf _
 * @since 4.3.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a map, else `false`.
 * @example
 *
 * _.isMap(new Map);
 * // => true
 *
 * _.isMap(new WeakMap);
 * // => false
 */
var isMap = nodeIsMap ? baseUnary(nodeIsMap) : baseIsMap;

var isMap_1 = isMap;

exports.isMap_1 = isMap_1;
