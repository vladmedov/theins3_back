'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _copyObject = require('./_copyObject.cjs');
const _getSymbolsIn = require('./_getSymbolsIn.cjs');

var copyObject = _copyObject._copyObject,
    getSymbolsIn = _getSymbolsIn._getSymbolsIn;

/**
 * Copies own and inherited symbols of `source` to `object`.
 *
 * @private
 * @param {Object} source The object to copy symbols from.
 * @param {Object} [object={}] The object to copy symbols to.
 * @returns {Object} Returns `object`.
 */
function copySymbolsIn(source, object) {
  return copyObject(source, getSymbolsIn(source), object);
}

var _copySymbolsIn = copySymbolsIn;

exports._copySymbolsIn = _copySymbolsIn;
